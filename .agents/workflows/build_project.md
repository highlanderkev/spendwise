---
description: Build the SpendWise Firebase Studio project
---

# Build Project Workflow

This workflow installs the required dependencies and creates an optimized production build of the Next.js application.

1. Ensure you are in the project root directory.
2. Install npm dependencies:
```bash
npm install
```
3. Run the Next.js build process:
```bash
npm run build
```
